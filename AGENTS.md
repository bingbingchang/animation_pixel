# animation_pixel — Agent启动规则

## 必须先读

依次读取 `README.md`、`docs/03_failure_catalog.md`、`docs/02_motion_spec_v2.md`、`configs/triple_slash_v2.json`、`cases/triple_slash_001/README.md`。需要外部案例时，再读取 `docs/04_open_source_review.md`，不要一次加载整个图库。

## 用户目标

批量生成有真人运动逻辑、爆发力和游戏美感的像素技能。审核以技能/语义段/代表性样本为单位，不能要求用户逐帧签字。用户期望真实换步、转体、稳定接地和清楚的目标意识；不是独立海报、信息图或武术教材。

## 本轮权限边界

仅归档、整理规范和研究开源方法。没有授权安装外部Skill、读取API密钥、额外运行付费生成、无限重试、抓取无关个人资料或变更其他仓库。

## 不可违反

- 当前三刀方向：屏幕投影左上→右下、右上→左下、低→高；目标始终在屏幕右侧。不要偷换回旧的“横斩/上挑/重劈”。
- 输出模式必须单一：`skeleton_only` / `character_only` / `fx_overlay` / `export_only`。骨架阶段不画头发、服装、围巾、彩色箭头、说明文字和UI。
- 左右肢体必须有永久ID和固定肩/髋连接。不得把当前屏幕左手位置当作恒定解剖左手。
- 头部要追踪目标，但不能让颈部扭成不可能的角度。必要时限制胸腔/骨盆反扭，或调整步法。
- `grounded=true`是这套技能的设计约束，不是宣称所有真实攻击都不腾空。至少一个支撑接触必须在连续运动中成立，而非只在关键节点成立。
- 原文件只追加归档，不覆盖。导出不得再次调用生图来“顺便修正”。最终RGBA须检查真实Alpha，不根据黑/白棋盘外观猜测透明。
- `PASS` / `FAIL` / `UNKNOWN`分开。看不出左右脚或脸朝向时记UNKNOWN，不编造角度和置信度。
- `prompt_requested`、`prompt_submitted`、`model_reported`、`seed`分别记录。历史图缺少实际最终提示词和种子，必须写unknown，不把后来重写的提示词倒填成原始提示词。
- 生成器自己的“自检通过”不是独立验证。结构校验、视觉审查、引擎播放测试分别记录。

## 版本与回归

每次只改可解释的一组因素。新增结果必须记录skill_id、父版本、输入参考哈希、规范版本、提示词版本、输出哈希、时序、失败标签、人工/自动证据来源。一次根因修复后，仍要对整套动作做回归，防止头修好、腿又坏。

分支和PR提交，默认不合并未验证的动作。既有失败案例必须保留。不得把用户说“有力量/有美感”扩大解释为“全部正确”。

## 预算与停止

付费调用前有明确授权和预算。建议局部自动修复最多2次，之后返回失败报告/UNKNOWN；此数量是本项目起始策略，非模型能力保证。权限、额度、模型不存在、政策错误不自动换模型绕过。

## 当前不能声称

没有实现动作求解器、AI腿身份跟踪、物理接触求解器或稳定套皮算法。文档中的Planner是设计合同，不是已经存在的程序。
